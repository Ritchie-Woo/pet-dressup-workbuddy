# Final 验收交付说明：两模块对齐

日期：2026-06-13

验收依据：

- `specs/two-module-alignment/acceptance.md`
- `specs/two-module-alignment/tasks.md`
- `docs/测试计划-两模块收缩版.md`
- `docs/两模块目标差异清单.md`

## 1. 验收结论

Batch 1-6 已按当前验收范围闭环，可以进入最终体验确认和代码整理。

验收 Gate 结果：

| Gate | 结论 | 说明 |
|---|---|---|
| Gate A Scope | 通过 | 底部主流程收缩为 `穿搭`、`地图`；旧团购、MBTI、支付、订单等不作为首版主流程暴露 |
| Gate B Dress-up | 通过 | 宠物列表、选中态编辑、资料保存、照片、公开状态、别人家的展示与点赞均闭环 |
| Gate C Map Place | 通过 | 地点分类、地图展示、详情来源/时间、缺失字段兼容、半自动采集方案均闭环 |
| Gate D Dog-Walking Privacy | 通过 | 先选狗狗、1 公里过滤、聚合气泡、头像-only 面板、15 分钟/重启状态、隐私限制均闭环 |
| Gate E Stability | 通过 | 本轮未发现登录、穿搭、地图、地点详情主路径白屏阻塞；弱/缺数据关键路径已覆盖 |

## 2. 已完成的 Batch

| Batch | 范围 | 结果 |
|---|---|---|
| Batch 1 | 信息架构收缩 | 通过 |
| Batch 2 | 穿搭宠物基础资料编辑 | 通过 |
| Batch 3 | 宠物照片与公开状态 | 通过 |
| Batch 4 | 别人家的公开宠物与点赞 | 通过 |
| Batch 5 | 宠物友好地图地点展示与详情 | 通过 |
| Batch 6 | 遛狗隐私与聚合 | 通过 |

## 3. 测试执行情况

主测试计划结果见 `docs/测试计划-两模块收缩版.md`：

| 模块 | 结果 |
|---|---|
| 信息架构 | 5/5 通过 |
| 穿搭-宠物资料 | 8/8 通过 |
| 穿搭-照片与公开 | 7/7 通过 |
| 穿搭-别人家的与点赞 | 7/7 通过 |
| 地图-地点展示 | 9/9 通过 |
| 地图-地点详情 | 6/6 通过 |
| 地图-遛狗 | 10/10 通过 |

辅助云函数检查：

| 编号 | 云函数/能力 | 结果 |
|---|---|---|
| CF-01/02 | `common-pet:list/update` | 通过 |
| CF-03/04/05 | `wp-recommend:list/like` | 通过 |
| CF-06/07 | `mp-place:list/detail` | 通过 |
| CF-08/09/10 | `mp-walk:start/query/stop` | 通过 |

本地自动检查：

- `node tests/mp-walk-privacy.test.js` 通过。
- `node --check miniprogram/mp/map/index.js` 通过。
- `node --check cloudfunctions/mp-walk-seed/index.js` 通过。

## 4. 云函数与数据库变更说明

### 云函数

| 云函数 | 变更 | 云端证据 |
|---|---|---|
| `common-pet` | 增加/兼容 `photos`、`isPublic`、`likeCount`；支持照片和公开状态更新；限制最多 5 张照片 | 部署请求 `88f0064b-2216-4eaa-b81d-551dddcb0cf2`；5 张/6 张/恢复写入均已验证 |
| `wp-recommend` | 列表改为真实 `common_pet` 公开宠物；新增一次性点赞；不补假数据 | 最新部署请求 `15c11dd3-267a-4bbe-9c35-f6882d19f2e4`；首次/重复点赞接口验证通过 |
| `mp-place` | 分类对齐；详情返回 `source/createdAt/updatedAt`；列表不暴露来源 | 部署请求 `10267c6d-3598-42f7-83a2-682f2159a5e3`；分类和详情接口验证通过 |
| `mp-walk` | 15 分钟过期；只允许狗狗；1 公里半径查询；聚合/模糊坐标；头像-only 返回；停止时删除活跃会话 | 部署请求 `7dcb9472-c3c9-4f27-9bcb-04dfed91d13e`；函数详情 RequestId `351b435b-ca59-4112-acb8-ac92ae3825e3`；用户态 start/query/stop 通过 |
| `mp-walk-seed` | 新增/更新玄武湖附近遛狗测试数据写入能力 | 代码部署 RequestId `93aacc1a-51fe-4416-baed-5f3f7eb0ebd9`；`seedXuanwu` 调用 RequestId `e5bb4e91-e3c2-4c84-9df3-9ea0beca2d3d` |

### 数据库

| 集合 | 变更/使用 |
|---|---|
| `common_pet` | 写入/兼容 `photos`、`is_public`、`like_count`；测试后恢复 `cookie.photos=[]`、`cookie.is_public=true`、点赞数到测试前状态 |
| `wp_pet_like` | 新增集合，用 `user_id + pet_id` 稳定 `_id` 实现一次性点赞；临时点赞记录已清理 |
| `mp_place` | 继续使用现有地点数据；详情支持来源和时间字段；未执行外部抓取 |
| `mp_active_walk` | 使用 `source: test_xuanwu_walk_seed` 的 3 条玄武湖临时活跃记录支持 Batch 6 手测 |
| `mp_walk_history` | `mp-walk:stop` 有效坐标时会写历史；历史写入失败不阻断 active session 清理 |

## 5. 测试数据说明

- 宠物资料/照片：使用当前账号宠物数据测试；照片上限、删除、公开状态已验证；临时上传测试文件已删除，测试字段已恢复。
- 公开发现：临时隐藏部分公开宠物验证少于 3 只不补假数据；随后已恢复公开状态。
- 点赞：使用管理端测试上下文验证首次点赞和重复点赞；测试后删除临时点赞记录并恢复 `cookie.like_count`。
- 地点：使用已有地点数据和分类接口验证；未执行抖音、小红书、公众号等外部采集。
- 遛狗：使用 `seedXuanwu` 写入 3 条测试记录。第 1/2 条距离约 0m/118m，第 3 条约 1981m；用户态只看到两条附近信息，符合 1 公里过滤。记录带 `pet_avatar_url: /assets/icons/walk.png`，用于头像-only 面板展示。

## 6. 未完成项或遗留风险

无当前阻塞项。

遗留风险/注意事项：

- 本次多人遛狗采用模拟活跃记录替代第二个真实账号，符合验收假设；上线前如有条件，建议再用真实双账号做一次冒烟。
- `mp_active_walk` 中仍保留 `source: test_xuanwu_walk_seed` 的玄武湖临时测试记录，便于最终体验确认；确认结束后可按需清理。
- `EDGE-01~04/08` 等泛化异常项不是本轮 Batch 1-6 的阻塞门槛，后续进入发布前回归时可继续补全。
- 当前工作树包含 Batch 1-6 多文件改动和新增云函数/文档，尚未提交；未按要求进行 git commit。

## 7. 需要用户最终体验确认的点

请最终确认以下体验细节是否可接受：

- 穿搭页选中宠物编辑图标的位置和点击手感。
- 宠物照片区域、公开/不公开开关、别人家的卡片视觉质量。
- 地图地点详情里的来源/收录/更新时间展示是否自然。
- 遛狗按钮位置、聚合气泡文案和显眼程度。
- 遛狗头像-only 面板是否足够清晰，且没有昵称、距离、路线等隐私信息。

## 8. 最终建议

当前可以进入代码整理、Review、提交或下一轮发布前回归。若要做一次更稳的上线前检查，建议只补一个轻量冒烟清单：登录进入穿搭、切地图、地点详情、开始/停止遛狗、查看附近气泡。
