# P0 任务清单 — 开团模块重构（v2.1）

> 基于 PRD `docs/PRD-开团模块重构-v2.md` v2.1 | 创建: 2026-06-08

**验证规则：** 每完成 10 个任务，回头验证已勾选的前 10 个。发现不一致则清除勾选、补充备注。

---

## 任务总览

| 编号 | 模块 | 任务数 |
|------|------|--------|
| A | app.json & 路由 | 4 |
| B | 移除旧代码 | 4 |
| C | 云函数变更 | 8 |
| D | request.js 重构 | 3 |
| E | 新建前端页面 | 5 |
| F | 修改现有页面 | 3 |

---

## A. app.json & 路由（PRD 二 §2.1-2.3、七 §7.1）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| A1 | 修改 tabBar.list：将第 3 位「订单」替换为「开团」(pagePath: `gb/groupbuy/index/index`, text: "开团", icon: `images/tab/gb.png` 暂用占位) | §2.1 L29-34, §2.3 L46-54 | ✅ | ⚠️ 图标暂用 gb.png 占位，后续替换为 groupbuy.png |
| A2 | 在 `pages` 数组中确认 `gb/order/list` 保留（非 TabBar），确保买家能通过 navigateTo 进入 | §2.1 L36, §2.2 L40-42 | ✅ | 已在 pages 数组第 10 行，tabBar 中已移除 |
| A3 | 在 `pages` 数组中注册 5 个新页面路由：`gb/groupbuy/index/index`、`gb/groupbuy/product/list/index`、`gb/groupbuy/product/edit/index`、`gb/groupbuy/order/list/index`、`gb/groupbuy/order/detail/index` | §7.1 L314-320 | ✅ | |
| A4 | 移除 `pages` 中 `gb/leader/*` 的 6 条旧路由 | §7.3 L335 | ✅ | |

---

## B. 移除旧代码（PRD 七 §7.3）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| B1 | 删除 `miniprogram/gb/leader/` 目录下全部 6 页面文件（index、product/list、product/edit、order/list、order/detail、progress） | §7.3 L335 | ✅ | |
| B2 | 修改 `my/index.js`：移除第 2 行 `checkRole` import、第 19 行 `_checkLeaderRole()` 调用、第 22-28 行 `_checkLeaderRole` 函数 | §7.3 L336 | ✅ | goOrders 已有指向 gb/order/list |
| B3 | 修改 `my/index.wxml`：移除团长入口卡片 | §7.3 L336 | ✅ | 我的订单入口已存在（原第 63 行 goOrders） |
| B4 | 删除 `database/phase4-leader.sql` 文件 | §7.3 L339 | ✅ | |

---

## C. 云函数变更（PRD 五 §5.2-5.4、九 §9.1-9.4）

### C1-C3: common-user（PRD 九 §9.1）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |
| ✅ | ⚠️ 保留 `role` 字段在 DB 中不动，仅不再写入，`checkRole` 相关的 `user.role \|\| 'user'` 自然移除 |

### C4-C7: gb-product（PRD 五 §5.2-5.4、九 §9.2）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |
| ✅ | |
| ✅ | |
| ✅ | |
| ✅ | |

### C8-C11: gb-order（PRD 五 §5.2-5.4、九 §9.3）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |
| ✅ | |
| ✅ | |
| ✅ | ⚠️ 同时保留原有 `leader_id` → `creator_id` 校验（§5.3 L181） |

### C12: gb-progress（PRD 五 §5.3-5.4、九 §9.4）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |

---

## D. request.js 重构（PRD 八 §8.1-8.2）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |
| ✅ | |
| ✅ | |

---

## E. 新建前端页面（PRD 三、四、六、七 §7.1）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | |
| ✅ | 可复用原 `gb/leader/product/list` 代码结构 |
| ✅ | 可复用原 `gb/leader/product/edit` 代码结构 |
| ✅ | 可复用原 `gb/leader/order/list` 代码结构 |
| ✅ | ⚠️ 发货弹窗需前端实现快递公司下拉选择器（代码表见 §6.6） |

---

## F. 修改现有页面（PRD 七 §7.2）

| # | 任务 | PRD 位置 | 状态 | 备注 |
|---|------|----------|------|------|
| ✅ | 与 B2/B3 同文件操作 |
| ✅ | |
| ✅ | |

---

## 验证记录

| 轮次 | 验证范围 | 结果 | 日期 |
|------|----------|------|------|
| — | — | — | — |

---

## 备注

- ⚠️ A1：图标 `images/tab/groupbuy.png` 需吴总提供，开发期间可暂用 `gb.png` 占位
- ⚠️ B2/F1：`my/index` 同时做移除旧和加新，属同一文件操作，合并处理
- ⚠️ C11：`ship` action 扩展时需同时保留 `creator_id` 校验（PRD §5.3 L181）
- ⚠️ E5：快递公司代码表已定义在 PRD §6.6，前端需硬编码为选择器数据源
- 📌 C1-C12 共 12 个云函数子任务跨越 4 个云函数，部署后需一并验证
