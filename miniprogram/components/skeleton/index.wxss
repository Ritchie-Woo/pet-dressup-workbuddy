/* components/skeleton/index.wxss — 简洁加载态 */
.loading-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 400rpx;
  gap: 24rpx;
}

.loading-text {
  font-size: 26rpx;
  color: var(--color-text-hint, #9B9993);
}
